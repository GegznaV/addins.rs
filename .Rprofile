# .First <- function(){
#     Sys.setlocale("LC_ALL", "Lithuanian")
#     cat("\nWelcome at", date(), "\n")
# }
#
# .Last <- function(){
#     cat("\nGoodbye at ", date(), "\n")
# }

options(repos = structure(c(CRAN = "https://cran.rstudio.com/")))
